# purplemux

**Claude Code, много задач одновременно. Быстрее.**

Все сессии на одном экране. Без обрывов — даже с телефона.

<a href="README.md">English</a> | <a href="README.ja.md">日本語</a> | <a href="README.zh-CN.md">简体中文</a> | <a href="README.zh-TW.md">繁體中文</a> | <a href="README.ko.md">한국어</a> | <a href="README.de.md">Deutsch</a> | <a href="README.es.md">Español</a> | <a href="README.fr.md">Français</a> | Русский | <a href="README.pt-BR.md">Português (Brasil)</a> | <a href="README.tr.md">Türkçe</a>

![purplemux](docs/images/screenshot.png)

![purplemux mobile](docs/images/screenshot-mobile.png)

## Установка

```bash
npx purplemux
```

Откройте [http://localhost:8022](http://localhost:8022) в браузере. Готово.

> Требуется Node.js 20+ и tmux. macOS или Linux.

Предпочитаете нативное приложение? Скачайте сборку macOS Electron из [последнего релиза](https://github.com/subicura/purplemux/releases/latest) (`.dmg` для Apple Silicon и Intel).

## Почему purplemux

- **Дашборд нескольких сессий** — одним взглядом видите статус «работает / ждёт ввода» по каждой сессии Claude Code
- **Мониторинг rate limit** — остаток за 5 часов / 7 дней и обратный отсчёт до сброса
- **Push-уведомления** — оповещения на десктопе и мобильном, когда задача завершена или нужен ввод
- **Мобильность и мульти-устройства** — доступ к одной и той же сессии с телефона, планшета или другого рабочего места
- **Live-вид сессии** — не нужно прокручивать вывод CLI: прогресс показывается в виде таймлайна

А также

- **Непрерывные сессии** — на базе tmux. Закрыли браузер — всё осталось на месте. При повторном подключении вкладки, панели и каталоги в точности там, где вы их оставили
- **Self-hosted и open source** — код и данные сессий никогда не покидают вашу машину. Никаких внешних серверов
- **Шифрованный удалённый доступ** — HTTPS откуда угодно через Tailscale

## Отличия от официального Remote Control

> Официальный Remote Control сфокусирован на удалённом управлении одной сессией. purplemux нужен, когда требуется управление множеством сессий, push-уведомления и устойчивые сессии.

## Возможности

### Терминал

- **Разделение панелей** — свободное горизонтальное / вертикальное разделение, изменение размера перетаскиванием
- **Управление вкладками** — много вкладок, перестановка перетаскиванием, автоматические заголовки на основе имени процесса
- **Горячие клавиши** — разделение, переключение вкладок, перемещение фокуса
- **Темы терминала** — тёмный / светлый режим, несколько цветовых тем
- **Рабочие пространства** — сохранение и восстановление раскладки панелей, вкладок и рабочих каталогов в пределах рабочего пространства
- **Просмотр git diff** — проверка git diff прямо в панели терминала. Переключение Side-by-side / Line-by-line, подсветка синтаксиса
- **Панель веб-браузера** — встроенный браузер рядом с терминалом для проверки результата разработки (Electron)

### Интеграция с Claude Code

- **Статус в реальном времени** — индикаторы «работает / ждёт ввода», переключение между сессиями
- **Live-вид сессии** — сообщения, вызовы инструментов, задачи, запросы разрешений, блоки thinking
- **Resume в один клик** — возобновляйте приостановленную сессию прямо из браузера
- **Автоматический resume** — при старте сервера восстанавливаются предыдущие сессии Claude
- **Быстрые промпты** — сохраняйте часто используемые промпты и отправляйте их в один клик
- **История сообщений** — повторно используйте прошлые сообщения
- **Статистика использования** — токены (input / output / cache read / cache write), стоимость, разбивка по проектам, ежедневные AI-отчёты
- **Rate limit** — остаток за 5 часов / 7 дней, обратный отсчёт до сброса

### Мобильность и доступность

- **Адаптивный UI** — терминал и таймлайн на телефоне и планшете
- **PWA** — добавление на главный экран, ощущение нативного приложения
- **Web Push** — уведомления приходят даже после закрытия вкладки
- **Синхронизация между устройствами** — изменения в рабочем пространстве отражаются в реальном времени
- **Tailscale** — HTTPS-доступ извне через туннель с шифрованием WireGuard
- **Аутентификация по паролю** — scrypt-хеширование, безопасно даже при внешнем доступе
- **Многоязычность** — 11 языков, включая 한국어, English, 日本語, 中文

## Поддерживаемые платформы

| Платформа | Статус | Примечания |
|---|---|---|
| macOS (Apple Silicon / Intel) | ✅ | Electron-приложение в комплекте |
| Linux | ✅ | Без Electron |
| Windows | ❌ | Не поддерживается |

## Подробности установки

### Требования

- macOS 13+ или Linux
- [Node.js](https://nodejs.org/) 20+
- [tmux](https://github.com/tmux/tmux)

### npx (самый быстрый способ)

```bash
npx purplemux
```

### Глобальная установка

```bash
npm install -g purplemux
purplemux
```

### Запуск из исходников

```bash
git clone https://github.com/subicura/purplemux.git
cd purplemux
pnpm install
pnpm start
```

Режим разработки:

```bash
pnpm dev
```

#### Уровень логирования

Общий уровень задаётся переменной `LOG_LEVEL` (по умолчанию `info`).

```bash
LOG_LEVEL=debug pnpm dev
```

Чтобы включить отдельные модули, перечислите пары `модуль=уровень` через запятую в `LOG_LEVELS`. Доступные уровни: `trace` / `debug` / `info` / `warn` / `error` / `fatal`.

```bash
# Трассировать только хуки Claude Code на уровне debug
LOG_LEVELS=hooks=debug pnpm dev

# Несколько модулей сразу
LOG_LEVELS=hooks=debug,status=warn pnpm dev
```

Модули, не указанные в `LOG_LEVELS`, используют значение `LOG_LEVEL`.

## Внешний доступ (Tailscale Serve)

```bash
tailscale serve --bg 8022
```

Доступ по адресу `https://<machine>.<tailnet>.ts.net`. Отключение:

```bash
tailscale serve --bg off 8022
```

## Безопасность

### Пароль

Пароль задаётся при первом входе. Он хешируется scrypt и сохраняется в `~/.purplemux/config.json`.

Чтобы сбросить, удалите `~/.purplemux/config.json` и перезапустите сервер — окно онбординга появится снова.

### HTTPS

По умолчанию используется HTTP. При внешнем доступе обязательно применяйте HTTPS:

- **Tailscale Serve** — шифрование WireGuard и автоматические сертификаты
- **Nginx / Caddy** — необходимо пробрасывать заголовки WebSocket upgrade (`Upgrade`, `Connection`)

### Каталог данных (`~/.purplemux/`)

| Файл | Описание |
|---|---|
| `config.json` | Данные аутентификации (хеш) и настройки приложения |
| `workspaces.json` | Раскладки рабочих пространств, вкладки, каталоги |
| `vapid-keys.json` | Ключи VAPID для Web Push (генерируются автоматически) |
| `push-subscriptions.json` | Данные push-подписок |
| `hooks/` | Пользовательские хуки |

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│  Browser                                                    │
│  ┌───────────┐ ┌───────────┐ ┌──────────┐ ┌─────────────┐   │
│  │  xterm.js │ │ Timeline  │ │ Status   │ │ Multi-device│   │
│  │  Terminal │ │           │ │          │ │ Sync        │   │
│  └─────┬─────┘ └─────┬─────┘ └────┬─────┘ └──────┬──────┘   │
└────────┼─────────────┼────────────┼──────────────┼──────────┘
         │ws           │ws          │ws            │ws
         │/terminal    │/timeline   │/status       │/sync
         ▼             ▼            ▼              ▼
┌─────────────────────────────────────────────────────────────┐
│  Node.js Server (:8022)                                     │
│  ┌──────────┐  ┌───────────────┐  ┌─────────────────────┐   │
│  │ node-pty │  │ JSONL Watcher │  │ Status Manager      │   │
│  │ PTY↔WS   │  │ File watch →  │  │ Process tree +      │   │
│  │ Binary   │  │ Parse → Send  │  │ JSONL tail analysis │   │
│  └────┬─────┘  └───────┬───────┘  └──────────┬──────────┘   │
└───────┼────────────────┼─────────────────────┼──────────────┘
        ▼                ▼                     ▼
┌─────────────────────────────────────────────────────────────┐
│  System                                                     │
│  tmux (purple socket)         Claude Code                   │
│  ┌────────┐ ┌────────┐       ┌────────────────────────────┐ │
│  │Session1│ │Session2│  ...  │ ~/.claude/sessions/        │ │
│  │ (shell)│ │ (shell)│       │ ~/.claude/projects/        │ │
│  └────────┘ └────────┘       │   └─ {project}/{sid}.jsonl │ │
│                              └────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**I/O терминала** — xterm.js подключается к node-pty через WebSocket, а node-pty — к сессиям tmux. Бинарный протокол обрабатывает stdin/stdout/resize с контролем backpressure.

**Определение статуса** — хуки событий Claude Code (`SessionStart`, `Stop`, `Notification`) мгновенно доставляют обновления по HTTP POST. Каждые 5–15 с проверяется дерево процессов и анализируются последние 8 КБ JSONL-файла.

**Таймлайн** — отслеживает JSONL-логи сессий в `~/.claude/projects/`, разбирает новые строки при изменении файла и транслирует структурированные записи в браузер.

**Изоляция tmux** — используется отдельный сокет `purple`, полностью независимый от уже работающего tmux. Без prefix-клавиши и статус-бара.

**Автовосстановление** — при старте сервера предыдущие сессии Claude восстанавливаются через `claude --resume {sessionId}`.

## License

[MIT](LICENSE)
