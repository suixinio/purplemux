{%- macro callout(type, title) -%}
<div class="callout callout--{{ type }}">
  {%- if title %}<div class="callout-title">{{ title }}</div>{% endif %}
  <div class="callout-body">{{ caller() }}</div>
</div>
{%- endmacro -%}
